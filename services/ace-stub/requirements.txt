# mfcommon is installed from source by the Dockerfile, not listed here --
# it is a path dependency inside this repo, not something to resolve from
# an index. For local development: pip install -e libs/mfcommon
fastapi>=0.110
httpx>=0.27
pydantic>=2.6
uvicorn[standard]>=0.29
