# ONLY this service's extras. The shared stack (fastapi, uvicorn, pydantic,
# httpx) and mfcommon come from microfinance-base, so listing them here would
# reinstall them and undo the point of the base image.
#
# For local development outside Docker: pip install -e libs/mfcommon
redis>=5.0
